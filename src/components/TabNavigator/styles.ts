import styled from "styled-components";
import { TabController, View, Text } from "react-native-ui-lib";

export const Tab = styled(TabController)``;

export const TabBar = styled(TabController.TabBar)``;

export const TabPage = styled(TabController.TabPage)``;
